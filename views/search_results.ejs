<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        .results-container {
            max-width: 900px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }

        .note-card {
            padding: 15px;
            border-bottom: 1px solid #e3e3e3;
        }

        .note-card:last-child {
            border-bottom: none;
        }

        .note-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 5px;
            color: #1a73e8;
        }

        .note-info {
            font-size: 14px;
            color: #555;
            margin-bottom: 5px;
        }

        .view-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 7px 15px;
            background: #1a73e8;
            color: white;
            border-radius: 5px;
            text-decoration: none;
        }

        .view-btn:hover {
            background: #1558b0;
        }

        .empty {
            text-align: center;
            padding: 30px;
            color: #777;
            font-size: 18px;
        }
    </style>

</head>
<body>

    <h1>Search Results for "<%= query %>"</h1>

    <div class="results-container">
        <% if (notes.length === 0) { %>
            <p class="empty">No notes found. Try another search.</p>
        <% } else { %>

            <% notes.forEach(note => { %>
                <div class="note-card">
                    <div class="note-title"><%= note.title %></div>

                    <div class="note-info"><strong>Subject:</strong> <%= note.subject %></div>
                    <div class="note-info"><strong>Branch:</strong> <%= note.branch %></div>
                    <div class="note-info"><strong>Semester:</strong> <%= note.semester %></div>

                    <a class="view-btn" href="/notes/<%= note._id %>">View Note</a>
                </div>
            <% }) %>
        <% } %>
    </div>

</body>
</html>