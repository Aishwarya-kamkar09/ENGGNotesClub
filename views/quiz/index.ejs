<!-- <!DOCTYPE html>
<html>
<head><title>Degree Level Quiz</title>
<link rel="stylesheet" href="/css/quiz.css">
</head>
<body>
<div class="quiz-container">
    <h1>🎓  Quiz </h1>
    <h4> Build a strong foundation by practicing with this quiz </h4>
   <br>
    

    <% if (error && error.length > 0) { %>
        <div class="alert-danger">
            ❌ <%= error %>
            <br><small>🙏 Sorry on this subject questions are not found</small>
        </div>
    <% } %>
    
    <form action="/quiz/generate" method="POST">
        <input type="text" name="subject" placeholder="Enter Subject name" required>
        <select name="limit">
            <option value="5">5 Questions</option>
            <option value="10">10 Questions</option>
        </select>
        <button> 🚀 Launch Quiz</button>
    </form>
</div>
</body>
</html>
 -->



















<!DOCTYPE html>
<html>
<head>
    <title>Degree Level Quiz</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
        }

        /* ---------------------------------------
           BEAUTIFUL ANIMATED BACKGROUND
        ---------------------------------------- */
        body {
            font-family: "Poppins", sans-serif;
            background: linear-gradient(120deg, #a7c0ff, #d4e1ff, #b9ccff);
            background-size: 300% 300%;
            animation: bgMove 12s ease infinite;

            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        @keyframes bgMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* ---------------------------------------
           QUIZ CARD
        ---------------------------------------- */
        .quiz-card {
            width: 100%;
            max-width: 520px;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(6px);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 50px rgba(0,0,0,0.15);

            animation: floatCard 3s ease-in-out infinite;
        }

        @keyframes floatCard {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }

        .logo {
            text-align: center;
            font-size: 34px;
            margin-bottom: 8px;
            color: #1e2240;
            font-weight: 600;
        }

        .tagline {
            text-align: center;
            color: #555;
            margin-bottom: 28px;
            font-size: 15px;
        }

        .alert-danger {
            background: #ffe5e5;
            color: #c40000;
            padding: 14px;
            border-radius: 12px;
            margin-bottom: 20px;
            border-left: 6px solid #c40000;
            font-size: 14px;
        }

        .input-box {
            margin-bottom: 22px;
        }

        .input-box label {
            font-size: 15px;
            color: #333;
            margin-bottom: 6px;
            display: block;
        }

        .input-box input,
        .input-box select {
            width: 100%;
            padding: 14px;
            border-radius: 12px;
            border: 1px solid #c5cee0;
            font-size: 16px;
            outline: none;
        }

        .input-box input:focus,
        .input-box select:focus {
            border-color: #4a6cf7;
            box-shadow: 0 0 6px rgba(74,108,247,0.40);
        }

        .primary-btn {
            width: 100%;
            padding: 15px;
            background: #4a6cf7;
            border: none;
            border-radius: 12px;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: 0.3s ease;
        }

        .primary-btn:hover {
            background: #2f4edc;
            transform: scale(1.02);
        }

        /* ---------------------------------------
           RESPONSIVE FIXES
        ---------------------------------------- */
        @media (max-width: 600px) {
            .quiz-card {
                padding: 30px;
                max-width: 95%;
            }

            .logo {
                font-size: 28px;
            }
        }

        @media (max-width: 400px) {
            .quiz-card {
                padding: 22px;
            }

            .logo {
                font-size: 24px;
            }

            .tagline {
                font-size: 13px;
            }
        }

    </style>
</head>
<body>

<div class="quiz-card">

    <h1 class="logo">🎓 Start Your Quiz</h1>
    <p class="tagline">Boost your knowledge with quick practice quizzes</p>

    <% if (error && error.length > 0) { %>
        <div class="alert-danger">
            ❌ <%= error %><br>
            <small>🙏 Sorry, no questions found for this subject.</small>
        </div>
    <% } %>

    <form action="/quiz/generate" method="POST">

        <div class="input-box">
            <label>Subject Name</label>
            <input type="text" name="subject" placeholder="Enter subject (e.g. Physics)" required>
        </div>

        <div class="input-box">
            <label>Select Question Limit</label>
            <select name="limit">
                <option value="5">5 Questions</option>
                <option value="10">10 Questions</option>
            </select>
        </div>

        <button class="primary-btn">🚀 Launch Quiz</button>

    </form>

</div>

</body>
</html>